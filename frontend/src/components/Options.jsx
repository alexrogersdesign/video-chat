import React from 'react';

/**
 *
 * @param {React.Childrem} children
 * @return {React.FC}
 */
export default function Options( {children}) {
  return (
    <div>
            Options
      {children}
    </div>
  );
}
